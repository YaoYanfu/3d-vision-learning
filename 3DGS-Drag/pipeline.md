输入：预训练 3DGS、原始多视角数据、控制点与目标点
  │
  ▼
用原始场景图像初始化图像缓冲区
初始 LoRA 微调扩散模型（200 次迭代
  │
  ▼
划分拖动阶段，计算当前阶段的中间目标点
  │
  ▼
Deformation Guidance
选择高斯 → 计算变形 → 复制粘贴 → 降低原高斯不透明度
  │
  ▼
多视角渲染
  │
  ▼
Diffusion Guidance
使用当前 LoRA 适配后的扩散模型修复图像
  │
  ▼
用修复图像监督 3DGS 优化
  │
  ├─ 按退火策略，重复“渲染 → 修复 → 3DGS 优化”
  │
  ▼
本阶段完成，重定位控制点
将新编辑结果加入图像缓冲区
继续 LoRA 微调（50 次迭代
  │
  ▼
进入下一个拖动阶段，直到完成